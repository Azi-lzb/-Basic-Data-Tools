# 真实业务数据（报送文件、校验结果、模板、历史库）—— 不得推入公开仓库
2026-07-31/
reference/

# 运行数据与历史：data/ 仅放行 config.xlsx，备份/模板索引/用户设置不上传
data/*
!data/config.xlsx
chatHistory/
历史审核说明.xlsx
dist/历史审核说明.xlsx

# 打包产物：dist（可直接运行的成品 EXE）放行，build（中间产物）不上传
build/
*.log

# Python 缓存
__pycache__/
*.py[cod]
.pytest_cache/
.pytest_*/
*.egg-info/
.venv/
venv/

# 系统 / 编辑器
Thumbs.db
.DS_Store
.idea/
.vscode/
.claude/

# ZCode 会话目录
.zcode/

# external 归档区：只放行已归档的 pywebview2 与 Flask（源码+dist），
# flutter 工具链(3.3G)/research/archives 不上传
external/*
!external/pywebview2/
!external/Flask/

# 运行时数据：历史审核配置（可能含真实机构历史问题）与流程配置不入库
历史审核配置.xlsx
流程配置.json

# 审核测试输出（含真实机构数据的审核副本）
测试执行结果*/
