# 基础数据审核工具：Claude 前端协作说明

## 任务边界

本项目采用前后端分工：

- Claude + DSV4Flash：只负责本地 HTML 前端的界面、交互和视觉呈现。
- Codex：只负责 Python 后端、Excel 审核逻辑、命名区域、历史库、测试和打包。

前端不是网页服务，不需要联网。程序通过 `pywebview` 在独立桌面窗口中加载本地 HTML；前端通过 `pywebview.api` 调用 Python 后端。

## Claude 可以修改的范围

- `src/base_audit/web/index.html`
- 如后续拆分：`src/base_audit/web/*.css`、`src/base_audit/web/*.js`

可以调整布局、颜色、字体、滚动、表格、按钮、文件勾选清单、提示文案和日志展示。

## Claude 不应修改的范围

- `src/base_audit/excel_com.py`
- `src/base_audit/service.py`
- `src/base_audit/history.py`
- `src/base_audit/template.py`
- `src/base_audit/name_config.py`
- `src/base_audit/models.py`
- `tools/` 下的模板迁移、打包和测试工具
- 正式模板、历史库和真实报送文件

不要改变 Python 接口名称、Excel 命名区域协议、审核副本生成逻辑或历史问题匹配逻辑。

## 当前前端接口

前端通过 `pywebview.api` 调用以下方法：

| 方法 | 用途 |
|---|---|
| `get_state()` | 读取当前表单、运行状态、日志和文件清单 |
| `update(values)` | 更新 `input`、`templateDir`、`template`、`external`、`output`、`recursive`、`outputPinned` |
| `choose_folder(field)` | 选择目录 |
| `choose_file(field)` | 选择 Excel 文件 |
| `recognize()` | 自动识别模板和待审核文件 |
| `set_selected_files(paths)` | 设置本次勾选的待审核文件；不会删除源文件 |
| `start('check')` | 审核前检查 |
| `start('audit')` | 兼容入口，等同执行流程“汇总核查表校验” |
| `start('summary')` | 兼容入口，等同执行流程“汇总校验结果说明” |
| `start_flow(flow_name)` | 按“执行流程”页中同名流程执行；主按钮应使用此接口 |
| `start_with_state(action, values, selected_files)` | 单次调用同步表单、勾选文件并启动兼容入口；Win7 包优先使用 |
| `start_flow_with_state(flow_name, values, selected_files)` | 单次调用同步表单、勾选文件并按流程名启动；Win7 包的主按钮应使用 |
| `open_config()` | 用系统默认程序打开 `data/config.xlsx` 快速查看配置，只读查看不修改内容 |
| `open_path(path)` | 用系统默认程序打开本地目录或文件：目录在资源管理器中打开，文件用关联程序打开（前端路径超链接不再用 file:// 导航，避免打开成 HTML 错误页） |
| `reset_config()` | 重置“模块化功能、执行流程”为内置默认值；保留“历史审核结果、自定义按钮”原样不动 |
| `open_user_guide()` | 用系统默认程序打开 EXE 同级的 `基础数据审核工具使用说明.docx` 使用说明（使用说明用 docx，不用 md） |
| `get_custom_features()` | 读取 `config.xlsx` 的“自定义按钮”页中“是否显示=是”的平铺按钮清单 |
| `run_custom(feature_name)` | 按功能名称找到对应流程后执行 |

`get_state()` 的关键字段：

- `busy`：任务是否运行中。
- `status`：状态提示。
- `log`：运行记录数组。
- `sourceFiles`：文件清单，元素含 `path`、`name`、`template`。
- `selectedFiles`：本次实际运行的文件绝对路径列表。
- `mixedTemplates`：本目录内识别到的模板名称列表；数量大于 1 时应提示用户取消勾选其他类型文件。
- `recursive`：是否递归查找源目录的子文件夹，默认 `true`，会记住上次选择；它同时影响待审核文件清单、模板推荐、审核执行和区域汇总。
- `outputPinned`：输出目录是否固定。`false` 时输出目录始终跟随源数据目录并自动设为 `源数据目录\\审核结果`；`true` 时保留用户指定路径。

“外部文件”用于完整审核和审核前检查：后端优先从模板公式识别需要的工作表，只把这些表复制到每个审核副本，用于模板公式引用；识别不到时复制外部文件全部工作表。前端只负责选择和展示路径；不要把它描述为“仅记录路径”。

后端新增 `initialize_config()` 接口，用于恢复 `config.xlsx` 的默认“模块化功能”和“执行流程”。前端可增加“初始化配置”按钮并调用 `pywebview.api.initialize_config()`；需要明确提示：它只补齐默认配置，不删除“自定义按钮”；旧版“历史审核记录”会迁移为“历史审核结果”，并保留已有的历史校验说明和审核意见。

后端提供 `organize_history_rule_numbers()` 接口。前端可增加独立按钮“整理历史规则编号”，调用 `pywebview.api.organize_history_rule_numbers()`；该动作只整理 `config.xlsx` 的“历史审核结果”，不会执行审核或修改源数据。按钮旁应提示用户先关闭已打开的 `config.xlsx`。

主按钮文字必须等于“执行流程”页中的“流程名”。点击确认后调用 `pywebview.api.start_flow('流程名')`，前端不得写死功能顺序或功能类型；后端按“顺序、功能名、启用、失败后处理”调度。默认流程不再保留独立“审核前检查”；两个主流程均以前置表结构比对开始。汇总说明流程中结构不匹配的文件会跳过，后端会在运行记录返回运行日志路径和跳过文件名。`start('audit')`、`start('summary')`仅为旧界面兼容。

为兼容 Win7 包的 pywebview 5，主按钮不要连续调用 `update()`、`set_selected_files()`、`start_flow()`。应改为一次调用：`pywebview.api.start_flow_with_state(flowName, values(), checkedPaths)`；其中 `checkedPaths` 为当前勾选文件的绝对路径数组。兼容旧按钮可调用 `start_with_state(action, values(), checkedPaths)`。调用成功后再 `refresh()`；不要在启动前额外 `await sync()` 或 `await sendSelection()`。

“执行流程”固定列为 `流程名、顺序、功能名、启用、失败后处理、是否输出结果、输出文件名、处理对象、备注`。前端只负责显示和传递流程名，不解析或拼接模块参数。模块的区域定义统一由“模块化功能”页维护。失败后处理仅支持“停止/跳过”。

每个流程运行一次生成一份运行日志 `流程名_运行日志_时间戳.xlsx`（永远生成，替代旧版合并的“审核前检查”报告），每个启用功能对应一个 sheet：
- 检查类（`检查_命名区域存在性`）：列出每个命名区域名所在的 `工作表、覆盖区域、结果`，区域缺失时记“缺失”；
- 表结构比对（`核对_表结构比对`）：列出每个报送文件的 `匹配结果、匹配率、匹配标签数、检查标签数、缺少工作表、说明`；
- 外部文件添加：列出每个报送文件 `复制的外部工作表、结果`；
- 公式校验复制：列出每个报送文件 `审核副本、复制公式数、结果`；
- 校验结果提取：列出本期每条问题的 `工作表、定位单元格、级别、校验字段、问题说明、当前值、对比值、差值、规则编号`；
- 合并同机构多表（`修改_合并同机构多表`）：按源文件主名下划线第一段分组，将同一机构的多个工作簿合并为一本；每张工作表重命名为“表格类型_原工作表名”，重名自动加序号。它必须是单独流程，供用户在合并文件上重建命名区域后再运行正常审核；
- 汇总功能（任意行/固定行/汇总表合并）：列出每张输出工作表 `输出工作表、行数`；
- 模板体检：保留模板健康检查诊断项。

“是否输出结果”只作用于会产出实际文件的功能，即 `外部文件添加`（输出同名阶段副本目录）、`公式校验复制`（输出同名审核副本目录）、`校验结果提取`（输出同名本期审核结果工作簿）、`合并同机构多表`（输出每机构一本合并工作簿），统一命名为 `序号_功能名_时间戳`；“输出文件名”列可为某步骤指定名字，填写后该步骤输出文件/目录改为 `输出文件名_时间戳`（默认配置把两个流程的最终结果预填为流程名）。“处理对象”列声明本步骤读哪份数据：留空=源文件，填前序功能名=读该功能产出的副本（校验结果提取填“公式校验复制”读审核副本、填“外部文件添加”读外部添加副本；任意行/固定行/汇总表合并填“公式校验复制”可对审核副本做汇总）；引用的功能名必须排在本步骤之前，主流程的校验结果提取预填“公式校验复制”、公式校验复制预填“外部文件添加”。处理对象只认流程中排在前面的外部文件添加/公式校验复制等会产副本的功能；引用不匹配或不产副本的功能（如检查类、核对类）时，自动跳过非副本功能，兜底到最近前一个产副本功能（之前没有则兜底到源数据目录），并在运行日志给出提示，不会中断流程。填“否”时这些中间文件处理完即清理。检查类与表结构比对不再单独输出报告。每个流程至少应有一个启用步骤输出结果。

右侧面板的“自定义按钮”平铺按钮读取 `data/config.xlsx` 的“自定义按钮”页，固定列为 `按钮名称、流程名称、是否显示、备注`。“按钮名称”是 WebView 按钮文字且应唯一；“流程名称”必须指向“执行流程”的流程名；“是否显示”填“是”才会出现。前端调用 `get_custom_features()` 后会得到扁平数组，每项含 `id`（等于按钮名称）、`name`、`flow`、`remark`；点击确认后调用 `run_custom(name)`。

汇总后端接口为 `AuditService.summarize_regions()`，命令行使用 `--region-summary`。工作台的“汇总校验结果说明”执行同名流程，使用“模块化功能”中的 `任意行汇总` 或 `固定行汇总`。两类汇总只配置主区域，程序自动读取同工作表的 `表头区域`；`单元格区域.字段名` 会作为固定字段写入该工作表的每条汇总明细。`全局单元格区域.字段名` 可定义在机构说明等公共工作表中，并自动附加到其他汇总表。一个模块匹配到多个区域时，最终输出表头相同才合并为一张模块表；表头不同则分别按原工作表名称输出。前端提示文字不得再描述在线抽验或历史比对。

在源数据目录附近增加复选项“递归子文件夹”：字体小、灰色、初始值取 `state.recursive`。切换时调用 `pywebview.api.update({recursive: checked})`。勾选时待审核清单、模板推荐、审核执行和区域汇总均递归查找所选源目录下的 `.xlsx`，未勾选时均只读取该目录第一层。程序会跳过其自行生成的“审核结果、机构审核副本、运行中间副本、测试结果”目录，避免重复审核输出文件。

“合并同机构多表”按钮是例外流程：只需要“源数据目录”和可选的“递归子文件夹”。前端切换到/点击该流程时，应弱化或隐藏模板文件、模板目录、外部文件和数据期相关控件，并说明“输出目录默认是源数据目录\\审核结果，可按书钉固定”。调用仍使用 `start_flow('合并同机构多表')`；后端会传入 `template_path=None`、忽略外部文件与数据期，前端无需提供占位模板。

## 前端交互要求

- 源数据目录是日常主要操作项；选择后应调用 `recognize()`。
- 模板目录、外部文件会记住上次选择，仍需保留手动修改入口。
- 输出目录默认是 `源数据目录\审核结果`，允许手动修改。
- 输出目录标题旁增加一个书钉按钮：未钉住时每次更换源数据目录都同步更新输出目录；点亮书钉后固定当前路径。按钮切换调用 `update({outputPinned: checked})`；用户手动修改输出目录后，前端应同步显示为已钉住。书钉应有明确的悬浮提示“固定输出目录 / 跟随源数据目录”。
- 在标题栏或“打开配置”旁增加紧凑入口 `📕`，悬浮提示“打开使用说明”，点击调用 `pywebview.api.open_user_guide()`。它只打开本地使用说明 `基础数据审核工具使用说明.docx`（使用说明一律用 docx，不用 md），不打开浏览器链接。
- “打开配置”按钮支持右键：在按钮附近弹出一个紧凑菜单项“重置配置”。点击后先以中文确认“将重置模块化功能和执行流程；历史审核结果、自定义按钮不会修改”，确认后调用 `pywebview.api.reset_config()`；右键菜单不得在普通左键点击时出现。
- “递归子文件夹”旁的说明应写清使用场景：汇总核查表校验通常不勾选；汇总校验结果说明在机构文件分散于子文件夹时通常勾选。不要在前端改变该选项的后端语义。
- 文件清单只能“取消本次勾选”，不得提供删除源文件功能。
- 长路径必须完整可见（可换行、悬浮提示或复制），不能仅依赖单行截断。
- 整个窗口和日志区都必须可滚动；日志必须可拖选复制。
- 任务执行时禁用会改变任务参数的操作；保留日志查看和复制。

## 修改后的自检

1. 前端能加载，无 JavaScript 语法错误。
2. 不调用未列出的 `pywebview.api` 方法。
3. 勾选文件后，`set_selected_files` 接收到的是绝对路径。
4. 不添加网络请求、服务器、浏览器地址栏或外部 CDN 依赖。
