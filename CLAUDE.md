# 基础数据审核工具：Claude 前端协作说明

## 任务边界

本项目采用前后端分工：

- Claude + DSV4Flash：只负责本地 HTML 前端的界面、交互和视觉呈现。
- Codex：只负责 Python 后端、Excel 审核逻辑、命名区域、历史库、测试和打包。

前端由本地 Flask 服务加载（`Flask/windows`、`Flask/uos` 两个外壳共用 `Flask/shared/frontend`），不需要联网；前端通过桥接层 `bridge.api` 调用 Python 后端（`window.bridge.api.方法名(...)`，就绪事件为 `bridgeready`）。历史文档中的 `pywebview.api` 写法即指该桥接层，`pywebview2` 项目已归档至 `external/pywebview2`，不再开发。

## Claude 可以修改的范围

- `frontend/web/index.html`
- 如后续拆分：`frontend/web/*.css`、`frontend/web/*.js`

可以调整布局、颜色、字体、滚动、表格、按钮、文件勾选清单、提示文案和日志展示。

## Claude 不应修改的范围

- `src/base_audit/excel_com.py`
- `src/base_audit/service.py`
- `src/base_audit/history.py`
- `src/base_audit/template.py`
- `src/base_audit/name_config.py`
- `src/base_audit/models.py`
- `tools/` 下的模板迁移、打包和测试工具
- 正式模板、历史库和真实报送文件

不要改变 Python 接口名称、Excel 命名区域协议、审核副本生成逻辑或历史问题匹配逻辑。

## UOS 计算引擎展示与发行约定

`uos_audit/` 是独立的 Flet + Python 原生版；统信/麒麟正式发行版只使用目标系统已安装的 LibreOffice Calc/UNO。前端只展示后端返回的 LibreOffice 可用状态、计算结果和错误定位，不得在前端自行切换、重算或判断公式结果。

- 正式界面只提供“LibreOffice Calc”计算/条件格式能力；不得展示或添加 `formulas`、IronCalc、EFC、Excel COM、WPS COM 的下拉选项、状态灯或回退文案。
- 前端应如实展示“LibreOffice 已使用 / 未安装 / 计算失败 / 错误定位”等状态，不能只显示“计算成功”。
- 界面文案不得承诺所有 Excel 公式均可兼容；对 `INDIRECT`、`OFFSET`、`CELL`、动态 `INDEX(...):INDEX(...)` 范围、外部工作簿引用和动态数组，应提示需要真实模板基准验证。
- Windows 正式界面仅展示 Microsoft Excel COM 与 WPS COM；不得展示 LibreOffice、`formulas`、IronCalc 或 EFC。Windows 与 UOS 的正式包均不得携带 `numpy`、`scipy`、`numpy-financial`、`schedula`、`regex`、`tqdm` 等纯 Python 公式引擎传递依赖。
- `external/research/engine_trials/` 是历史研究资产，前端不修改也不在正式界面暴露其引擎或测试结论。

## UOS 前端设计规范（企业级 B 端管理平台）

`uos_audit/` 的 Flet 工作台按企业级 B 端管理平台标准设计，风格专业、干净、高效，
信息密度适中、操作路径清晰，参考飞书后台、Jira 工作流、Figma 管理界面。

### 配色与字体

- 主色调：深蓝 `#1A3C6E` + 浅灰背景 `#F5F7FA`。
- 主按钮：`#1A3C6E` 背景、白色文字、圆角 6px，悬停加深（`#15305A`）。
- 次要按钮：白底、`#1A3C6E` 边框与文字、圆角 6px。
- 危险操作（删除）：`#E74C3C`；成功：`#27AE60`；警告：`#F39C12`；信息：`#3498DB`。
- 字体：中文 思源黑体（Noto Sans CJK SC），英文/数字 Inter；回退到微软雅黑。
- 卡片阴影 `0 2px 8px rgba(0,0,0,0.08)`，圆角 8px。
- 间距：页面内边距 24px、卡片间距 16px、组件间距 12px、正文行高 1.6。
- 信息层级：一级标题 18px 加粗 → 二级标题 15px 加粗 → 正文 14px → 辅助说明 12px 灰色。

### 全局布局

- 左侧固定导航栏（宽 220px），深色背景 `#1A3C6E`、白色文字，图标 + 文字，
  选中项高亮：`主工作台 / 常规设置 / 操作指引 / 模块说明 / 功能配置 / 流程编排`。
- 右侧主内容区（剩余宽度），浅灰背景 `#F5F7FA`，内边距 24px。
- 顶部面包屑导航 + 页面标题行。
- 设置中心不再是弹窗，而是侧栏导航下的独立页面。

### 页面规范

- **主工作台（主界面）**：左侧源数据配置区（约 40% 宽）+ 右侧执行结果区（约 60% 宽）。
  左侧为目录选择器（路径输入框 + 选择按钮）与待审核文件清单（含全选）；
  右侧顶部显示当前执行流程名，中间为深色运行日志控制台（`#1E1E1E` 背景、绿色终端字体、
  时间戳 + 记录），底部为“执行流程”主按钮与计算引擎状态提示条
  （如 `LibreOffice✓` / `LibreOffice✗（需安装）` 及环境配置建议）。
- **常规设置 / 计算与运行**：全宽表单，标签在左、控件在右；分组
  “计算引擎设置（下拉 + 已安装/未找到状态点）”“运行选项（开关 + 右侧说明）”
  “环境提示（浅黄提示框 + 检查环境按钮）”。
- **模块说明**：左栏约 1/3 按类型分组的名称列表，右栏约 2/3 当前项说明，只读。
- **功能配置**：左右分栏；右侧为编辑表单（功能名/执行模块下拉/命名区域名/输入/输出/备注）
  + 新建/保存/复制/删除；选中“修改_组合工作表”时右侧维护分组方案。
- **流程编排**：左栏流程列表（宽 280px，卡片样式，每行含名称 + 启用/禁用状态标签，
  悬停浮现 编辑/复制/删除）；右栏当前步骤设置采用“三段式”卡片
  （输入 → 执行模块 → 输出），卡片间用灰色箭头连接；底部操作栏固定
  （恢复默认/取消/保存，保存为主按钮）。
- 每个设置页面底部有操作栏：右侧排列“恢复默认”（文字链接）、“取消”（次要）、
  “保存”（主按钮）。

### 交互与体验

- 列表项悬停高亮并浮现操作图标；流程步骤支持拖拽排序（拖拽手柄）。
- 配置变更时保存按钮高亮提示（黄色圆点标记）；输入框支持 Enter 快速保存。
- 错误/警告信息用彩色状态条在对应区域顶部显示。
- 加载状态使用骨架屏，按钮点击后显示 Loading。
- 图标为线性风格，统一 16–20px（Feather/Lucide 风格）。

### 实现约束

- 只修改 `uos_audit/src/uos_audit/flet_app.py`（Flet 前端）；不改 `web_app.py` 后端接口
  与计算引擎逻辑；不修改 `uos_audit/engine_trials/`。
- 设置中心仍通过 `get_config_editor_data()` 读取、`validate_config_editor_draft()` 校验、
  `save_config_editor_draft()` 保存，字段协议不变。

## 当前前端接口

前端通过 `bridge.api` 调用以下方法：

| 方法 | 用途 |
|---|---|
| `get_state()` | 读取当前表单、运行状态、日志和文件清单 |
| `update(values)` | 更新 `input`、`templateDir`、`template`、`external`、`output`、`recursive`、`outputPinned`、`writeFlowLogs`、`calculationEngine`、`showCustomFeatures` |
| `choose_folder(field)` | 选择目录 |
| `choose_file(field)` | 选择 Excel 文件 |
| `recognize()` | 自动识别模板和待审核文件 |
| `set_selected_files(paths)` | 设置本次勾选的待审核文件；不会删除源文件 |
| `start('check')` | 审核前检查 |
| `start('audit')` | 兼容入口，等同执行流程“汇总核查表校验” |
| `start('summary')` | 兼容入口，等同执行流程“汇总校验结果说明” |
| `start_flow(flow_name)` | 按内置或自定义流程名执行；主按钮应使用此接口 |
| `start_with_state(action, values, selected_files)` | 单次调用同步表单、勾选文件并启动兼容入口；Win7 包优先使用 |
| `start_flow_with_state(flow_name, values, selected_files)` | 单次调用同步表单、勾选文件并按流程名启动；Win7 包的主按钮应使用 |
| `create_template_merge()` | 交互式制作联合模板：连续选择底稿模板和多个来源工作簿，不使用页面路径、自动识别或配置流程 |
| `open_path(path)` | 用系统默认程序打开本地目录或文件：目录在资源管理器中打开，文件用关联程序打开（前端路径超链接不再用 file:// 导航，避免打开成 HTML 错误页） |
| `reset_config()` | 还原模块、流程和按钮的初始设置；历史审核结果不变 |
| `open_user_guide()` | 用系统默认程序打开 EXE 同级的 `基础数据审核工具使用说明.docx` 使用说明（使用说明用 docx，不用 md） |
| `get_custom_features()` | 读取 `流程配置.json` 中勾选“显示在主界面”的自定义流程清单 |
| `run_custom(feature_name)` | 按流程名称执行显示在主界面的自定义流程 |
| `get_config_editor_data()` | 读取设置中心所需的功能、常用流程、自定义流程、流程显示设置及“组合工作表分组方案”；不返回历史审核结果 |
| `validate_config_editor_draft(draft)` | 校验设置中心草稿，不写入文件；返回 `{valid, errors}` |
| `save_config_editor_draft(draft)` | 保存功能、流程和流程显示设置；历史审核结果不改动；返回 `{ok, errors, data}` |

`get_state()` 的关键字段：

- `busy`：任务是否运行中。
- `status`：状态提示。
- `log`：运行记录数组。
- `sourceFiles`：文件清单，元素含 `path`、`name`、`template`。
- `selectedFiles`：本次实际运行的文件绝对路径列表。
- `mixedTemplates`：本目录内识别到的模板名称列表；数量大于 1 时应提示用户取消勾选其他类型文件。
- `recursive`：是否递归查找源目录的子文件夹，默认 `true`，会记住上次选择；它同时影响待审核文件清单、模板推荐、审核执行和区域汇总。
- `outputPinned`：输出目录是否固定。`false` 时输出目录始终跟随源数据目录并自动设为 `源数据目录\\执行结果`；`true` 时保留用户指定路径。
- `calculationEngine`：计算引擎偏好，固定取值为`自动`、`Microsoft Excel`、`WPS 表格`。自动时优先 Excel 再尝试 WPS；指定后只启动对应引擎，便于排查同机两套软件公式结果不一致的问题。
- `showCustomFeatures`：是否在主界面显示整块自定义流程入口；默认 `true`。它不改变各流程自身的“显示在主界面”设置。

## 设置中心接口与前端约定

设置中心打开时调用 `get_config_editor_data()`。返回字段：

- `featureTypes`：程序内置“模块”的类型清单，只读展示；
- `modules`：业务“功能”的有效配置列表（内部字段沿用 modules），允许编辑；每个功能选择一个模块类型并填写命名区域。
- `defaultFlows`：常用流程的有效步骤，允许编辑；保存后覆盖初始默认值。
- `customFlows`：可编辑的自定义流程步骤，字段等于 `flowHeaders`；
- `flowDisplay`：`{流程名: true}` 形式的流程显示设置；仅自定义流程勾选后显示在主界面，按钮文字固定为流程名。
- `defaultFlowNames`：默认流程名清单，用于将常用流程与自定义流程分组展示，以及恢复初始流程。
- `combineSheetsPlans`：可保存、切换的“组合工作表”分组方案列表；每项含稳定 `id`、`name`、`mode`、`pattern`、`groups`。
- `activeCombineSheetsPlanId`：当前启用方案的 `id`；执行“组合工作表”时只使用这一套方案。
- `combineSheetsPlan`：当前启用方案的只读兼容字段，新前端不应写回它。
- `combineSheetsModes`：分组方式选项，固定为 `regex`（文件名正则表达式）和 `name`（按名称/关键字分组）。

保存时传：`{customModules: [...], customFlows: [...], flowDisplay: {...}, combineSheetsPlans: [...], activeCombineSheetsPlanId: '...'}`。模块配置固定字段为“功能名、执行模块、命名区域名、输入、输出、备注”；“执行模块”必须从 `featureTypes` 下拉选择。“输入 / 输出”仅展示模块通常读取和产生的内容，不参与调度。`customModules`、`customFlows` 包含常用流程/模块的当前有效值，用于覆盖初始默认值。可先调用 `validate_config_editor_draft(draft)` 显示中文错误，再调用 `save_config_editor_draft(draft)`。

设置中心进阶使用按三层组织：**模块说明 → 功能配置 → 流程编排**。模块是代码中固定的程序能力，只读查看；功能是“选择模块 + 功能名称 + 命名区域 + 备注”的业务配置，可新建、编辑、复制。模块说明和功能配置均使用左右分栏：左侧约三分之一是按类型分组的名称列表，右侧约三分之二显示当前项的说明或编辑表单；选中“组合工作表”功能时，分组方案也在其右侧作为专用参数维护，不再单列页签。流程编排使用工作台：左侧为常用/自定义流程列表，中间是按顺序排列的步骤卡片及可加入的功能库，右侧为当前步骤设置。自定义流程在右侧可勾选“显示在主界面”，主界面入口文字固定为流程名；不再维护独立自定义按钮。前端不得直接操作 Excel，也不得把历史审核记录纳入草稿。

“外部文件”用于完整审核和审核前检查：后端优先从模板公式识别需要的工作表，只把这些表复制到每个审核副本，用于模板公式引用；识别不到时复制外部文件全部工作表。前端只负责选择和展示路径；不要把它描述为“仅记录路径”。

后端 `initialize_config()` 会首次将旧 Excel 中的流程/按钮迁移到 `data/流程配置.json`，并将旧 `data/config.xlsx` 或 `data/历史审核说明.xlsx` 移到程序根目录的 `历史审核说明.xlsx`；已有历史说明和审核意见保留。历史审核说明由用户直接在 Excel 中维护，设置中心不展示、预览或整理该文件。程序启动时会把 `data` 设为 Windows 隐藏目录。

主按钮文字必须等于“执行流程”页中的“流程名”。点击确认后调用 `bridge.api.start_flow('流程名')`，前端不得写死功能顺序或功能类型；后端按“顺序、功能名、启用、失败后处理”调度。默认流程不再保留独立“审核前检查”；两个主流程均以前置表结构比对开始。汇总说明流程中结构不匹配的文件会跳过，后端会在运行记录返回运行日志路径和跳过文件名。`start('audit')`、`start('summary')`仅为旧界面兼容。

主按钮应一次调用：`bridge.api.start_flow_with_state(flowName, values(), checkedPaths)`；其中 `checkedPaths` 为当前勾选文件的绝对路径数组。兼容旧按钮可调用 `start_with_state(action, values(), checkedPaths)`。调用成功后再 `refresh()`；不要在启动前额外 `await sync()` 或 `await sendSelection()`。

“执行流程”固定列为 `流程名、顺序、功能名、启用、失败后处理、是否输出结果、输出文件名、输入、输出、备注`。前端只负责显示和传递流程名，不解析或拼接模块参数。模块的区域定义统一由“模块化功能”页维护。失败后处理仅支持“停止/跳过”。执行模块以 `检查_` 或 `核对_` 开头时，“是否输出结果”固定显示为“否”且不可编辑；该类功能只写入运行日志。流程步骤编辑器应提供“输入”和“输出”普通文本输入框。

每个流程运行一次生成一份运行日志 `流程名_运行日志_时间戳.xlsx`（永远生成，替代旧版合并的“审核前检查”报告），每个启用功能对应一个 sheet：
- 检查类（`检查_命名区域存在性`）：列出每个命名区域名所在的 `工作表、覆盖区域、结果`，区域缺失时记“缺失”；
- 表结构比对（`核对_表结构比对`）：列出每个报送文件的 `匹配结果、匹配率、匹配标签数、检查标签数、缺少工作表、说明`；
- 外部文件添加：列出每个报送文件 `复制的外部工作表、结果`；
- 公式校验复制：列出每个报送文件 `审核副本、复制公式数、结果`；
- 校验结果提取：列出本期每条问题的 `工作表、定位单元格、级别、校验字段、问题说明、当前值、对比值、差值、规则编号`；
- 组合联合核查表（`修改_组合联合核查表`）：按源文件主名下划线第一段分组，将同一机构的多个工作簿合并为一本；工作表保留原名，同名工作表只保留先复制的一张、后续跳过。运行日志列出实际合并的工作表名称；
- 组合工作表（`修改_组合工作表`）：按设置中心的分组方案将任意工作簿组合为一本；默认方案为文件名正则提取 `(?P<组合>...)`，也支持按名称/关键字分组。文件未命中、同时命中多个匹配或规则不完整时，后端会明确报错而不会错误合并；
- 汇总功能（任意行/固定行/汇总表合并）：列出每张输出工作表 `输出工作表、行数`；
- 模板体检：保留模板健康检查诊断项。

“是否输出结果”只作用于会产出实际文件的功能，即 `外部文件添加`（输出同名阶段副本目录）、`公式校验复制`（输出同名审核副本目录）、`校验结果提取`（输出同名本期审核结果工作簿）、`组合联合核查表`（输出每机构一本合并工作簿），统一命名为 `序号_功能名_时间戳`；“输出文件名”列可为某步骤指定名字，填写后该步骤输出文件/目录改为 `输出文件名_时间戳`（默认配置把两个流程的最终结果预填为流程名）。“处理对象”列声明本步骤读哪份数据：留空=源文件，填前序功能名=读该功能产出的副本（校验结果提取填“公式校验复制”读审核副本、填“外部文件添加”读外部添加副本；任意行/固定行/汇总表合并填“公式校验复制”可对审核副本做汇总）；引用的功能名必须排在本步骤之前，主流程的校验结果提取预填“公式校验复制”、公式校验复制预填“外部文件添加”。处理对象只认流程中排在前面的外部文件添加/公式校验复制等会产副本的功能；引用不匹配或不产副本的功能（如检查类、核对类）时，自动跳过非副本功能，兜底到最近前一个产副本功能（之前没有则兜底到源数据目录），并在运行日志给出提示，不会中断流程。填“否”时这些中间文件处理完即清理。检查类与表结构比对不再单独输出报告。每个流程至少应有一个启用步骤输出结果。

右侧面板的自定义流程入口读取 `data/流程配置.json` 的 `flowDisplay`。仅自定义流程可勾选显示，入口文字、`id`、`name` 与 `flow` 均为流程名；点击确认后调用 `run_custom(流程名)`。旧版“自定义按钮”配置首次读取时自动迁移为对应流程的显示设置。

汇总后端接口为 `AuditService.summarize_regions()`，命令行使用 `--region-summary`。工作台的“汇总校验结果说明”执行同名流程，使用“模块化功能”中的 `任意行汇总` 或 `固定行汇总`。两类汇总只配置主区域，程序自动读取同工作表的 `表头区域`；`单元格区域.字段名` 会作为固定字段写入该工作表的每条汇总明细。`全局单元格区域.字段名` 可定义在机构说明等公共工作表中，并自动附加到其他汇总表。一个模块匹配到多个区域时，最终输出表头相同才合并为一张模块表；表头不同则分别按原工作表名称输出。前端提示文字不得再描述在线抽验或历史比对。

在源数据目录附近增加复选项“递归子文件夹”：字体小、灰色、初始值取 `state.recursive`。切换时调用 `bridge.api.update({recursive: checked})`。勾选时待审核清单、模板推荐、审核执行和区域汇总均递归查找所选源目录下的 `.xlsx`，未勾选时均只读取该目录第一层。程序会跳过目录名包含 `_skip` 的任何目录；旧版 `执行结果` 目录也固定跳过。用户可自行将不想处理的目录改名为如 `历史文件_skip`。

“组合联合核查表”是例外流程：只需要“源数据目录”和可选的“递归子文件夹”。前端点击该按钮时，应弱化或隐藏模板文件、模板目录、外部文件和数据期相关控件，并说明“输出目录默认是源数据目录\\执行结果，可按书钉固定”。它调用 `start_flow_with_state(流程名, values(), checkedPaths)`；后端会传入 `template_path=None`、忽略外部文件与数据期，前端无需提供占位模板。主界面不再单独提供“组合工作表”按钮；“组合工作表”（`修改_组合工作表`）作为内置模块保留在模块页，供用户在流程编排中拖入自定义流程调用。该模块同样只需源数据目录与递归选项，不读取模板、外部文件或数据期，分组方案来自设置中心的“组合工作表分组方案”。

“组合联合核查表”按钮的右键菜单应增加低频入口“制作联合模板”。点击后，前端必须先弹出中文说明和确认按钮，明确说明这是低频的模板制作动作、不读取页面路径或自动识别模板；确认后只调用 `bridge.api.create_template_merge()`，不得先调用 `update()`、`recognize()`、`set_selected_files()` 或 `start_flow()`。后端依次弹出两次文件选择框：**第 1 步，选择基准模板**（它只会另存为最终联合模板，原文件不修改；应选择包含集中系统数据、参照表等公共依赖工作表的模板）；**第 2 步，批量选择并入模板**（其独有工作表将复制到联合模板，不要重复选择基准模板）。前端说明中必须区分这两个角色，并提示用户不要将普通业务模板误选为基准模板。输出固定在基准模板同级 `联合模板` 目录，包含联合模板文件与“联合模板检查报告”；同名工作表保留基准/先复制版本并跳过后续版本。所有工作表复制完成后，后端会将全部外部工作簿公式引用统一改为工作簿内引用；若对应工作表没有被合并，最终会以 `#REF!` 写入报告提示处理。该动作不是日常源数据合并，不使用任何工作台路径或自动匹配模板。

## 前端交互要求

- 源数据目录是日常主要操作项；选择后应调用 `recognize()`。
- 模板目录、外部文件会记住上次选择，仍需保留手动修改入口。
- 输出目录默认是 `源数据目录\执行结果`，允许手动修改。
- 输出目录标题旁增加一个书钉按钮：未钉住时每次更换源数据目录都同步更新输出目录；点亮书钉后固定当前路径。按钮切换调用 `update({outputPinned: checked})`；用户手动修改输出目录后，前端应同步显示为已钉住。书钉应有明确的悬浮提示“固定输出目录 / 跟随源数据目录”。
- 在标题栏或“打开配置”旁增加紧凑入口 `📕`，悬浮提示“打开使用说明”，点击调用 `bridge.api.open_user_guide()`。它只打开本地使用说明 `基础数据审核工具使用说明.docx`（使用说明一律用 docx，不用 md），不打开浏览器链接。
- 主界面不再显示“打开配置”及其右键菜单。设置中心不展示历史审核说明；恢复默认设置、自定义功能全局显示开关和日志展示方式均收口到设置中心；“恢复默认设置”需中文二次确认后调用 `bridge.api.reset_config()`，它不会修改历史审核说明。日志展示方式仅是前端过滤 detail 日志的本地设置，无需后端接口。
- 设置中心增加“操作指引”页：仅说明汇总核查表校验、汇总校验结果说明、组合联合核查表和制作联合模板的准备、步骤、输出与提示。自定义流程、组合工作表及模块说明均属于进阶设置，不列入操作指引。该页为纯静态前端内容，不调用后端、不修改配置，详细内容以 `给Claude的设置中心提示词.md` 为准。
- “递归子文件夹”旁的说明应写清使用场景：汇总核查表校验通常不勾选；汇总校验结果说明在机构文件分散于子文件夹时通常勾选。不要在前端改变该选项的后端语义。
- 文件清单只能“取消本次勾选”，不得提供删除源文件功能。
- 长路径必须完整可见（可换行、悬浮提示或复制），不能仅依赖单行截断。
- 整个窗口和日志区都必须可滚动；日志必须可拖选复制。
- 任务执行时禁用会改变任务参数的操作；保留日志查看和复制。

## 修改后的自检

1. 前端能加载，无 JavaScript 语法错误。
2. 不调用未列出的 `bridge.api` 方法。
3. 勾选文件后，`set_selected_files` 接收到的是绝对路径。
4. 不添加网络请求、服务器、浏览器地址栏或外部 CDN 依赖。
