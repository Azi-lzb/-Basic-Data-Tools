# 任务：设置中心改为“内置模块 + 自定义流程 + 自定义按钮”

只修改 `src/base_audit/web/` 内的前端文件。不要修改 Python、Excel、JSON、打包文件；不要使用网络、CDN 或第三方在线资源。

## 最终数据边界

```text
data/历史审核说明.xlsx  只保存“历史审核结果”，由用户直接在 Excel 中维护
data/流程配置.json     保存自定义流程、自定义按钮（后端维护）
data/用户设置.json     保存常用路径、递归、计算引擎等本机偏好
```

前端不得读写文件，只能调用 `pywebview.api`。

核心原则：**内置模块和默认流程只展示、不可修改；允许用户新增、编辑、删除自定义模块。自定义模块的“功能类型”只能从内置类型中选择，其他字段可自定义。用户再通过 UI 创建自定义流程和自定义按钮。**

## 接口

```js
const state = await pywebview.api.get_state()
// state.calculationEngine: 自动 / Microsoft Excel / WPS 表格

await pywebview.api.update({
  calculationEngine: '自动', // 或 Microsoft Excel / WPS 表格
  recursive: true,
  writeFlowLogs: true
})

const data = await pywebview.api.get_config_editor_data()
```

`data` 关键字段：

```js
{
  modules: [...],       // 内置模块 + 自定义模块；行带“只读”: "是"或"否"
  defaultFlows: [...],  // 内置流程，只读步骤
  customFlows: [...],   // 可编辑的自定义流程步骤
  buttons: [...],       // 可编辑的自定义按钮
  defaultFlowNames: ["汇总核查表校验", "汇总校验结果说明", "组合联合核查表", "组合工作表"],
  featureTypes: [...],
  mappingHeaders: [...],
  flowHeaders: ["流程名", "顺序", "功能名", "启用", "失败后处理", "是否输出结果", "输出文件名", "处理对象", "备注"],
  buttonHeaders: ["按钮名称", "流程名称", "是否显示", "备注"],
  storage: "流程配置.json",
  combineSheetsPlans: [{
    id: "default",
    name: "按文件名正则提取组合",
    mode: "regex", // regex / name
    pattern: "(?P<组合>.+?)_金融基础数据-",
    groups: []
  }],
  activeCombineSheetsPlanId: "default",
  combineSheetsModes: [
    {id: "regex", name: "正则表达式"},
    {id: "name", name: "按名称分组"}
  ]
}
```

保存前校验：

```js
const draft = {
  customModules: state.customModules,
  customFlows: state.customFlows,
  buttons: state.buttons,
  combineSheetsPlans: state.combineSheetsPlans,
  activeCombineSheetsPlanId: state.activeCombineSheetsPlanId
}
const validation = await pywebview.api.validate_config_editor_draft(draft)
// { valid: true, errors: [] }
```

保存：

```js
const result = await pywebview.api.save_config_editor_draft(draft)
// 成功：{ ok: true, errors: [], data: {...} }
```

不要提交 `modules`（它是内置模块与自定义模块的展示合并结果）、`defaultFlows` 或历史审核结果。提交 `customModules`、`customFlows`、`buttons`。

成功后调用现有 `refreshCustomButtons()`，立即刷新主界面的自定义按钮。

## 设置中心布局

在原“打开配置”附近只保留“设置”按钮，打开较宽抽屉或弹窗：

```text
┌──────────────────────────────────────────────────────┐
│ 设置中心                                         ×    │
├──────────────┬───────────────────────────────────────┤
│ 常规设置     │ 当前页内容                            │
│ 内置模块     │                                       │
│ 流程编排     │                                       │
│ 自定义按钮   │                                       │
├──────────────┴───────────────────────────────────────┤
│ 取消                                  保存自定义设置 │
└──────────────────────────────────────────────────────┘
```

在左侧页签增加 **操作指引**，放在“常规设置”之后、“内置模块”之前。它是纯前端静态说明，不调用后端、不读取 Excel、不写入设置；内容区必须可滚动。

## 操作指引页：流程优先、模块进阶

普通用户首先需要知道“要完成什么”，而不是理解模块。因此默认展示“常用流程”，模块说明放在页面下方折叠区或“进阶：模块说明”二级入口。

页面顶部可提供两个切换项：`常用流程`（默认）/ `进阶模块`。每一项用可展开卡片或左侧列表 + 右侧说明呈现；不要堆成长篇文字。流程卡片要显示适用场景、准备内容、操作步骤、输出结果和常见提示。

### 常用流程（默认展开）

#### 1. 汇总核查表校验

适用：审核同一种基础数据报表，生成“本期审核结果”和各机构审核副本。

```text
准备：选择源数据目录；确认模板目录已存放对应模板；如模板公式依赖集中数据/参照表，再选择外部文件。
步骤：① 选择源数据目录；② 首次选择后核对自动识别的模板，必要时手动选择；③ 在待审核文件中取消本次不需处理的文件；④ 点击“汇总核查表校验”。
输出：执行结果目录下的本期审核结果、需要保留时的审核副本、流程运行日志。
提示：原始报送文件不会修改；通常不需要勾选“递归子文件夹”。
```

#### 2. 汇总校验结果说明

适用：汇总机构报送的任务说明、业务说明、本地校验结果等文件；机构文件放在子文件夹时尤其适用。

```text
准备：选择包含校验结果及报送说明的源数据目录，并选择对应的汇总模板。
步骤：① 选择源数据目录；② 勾选“递归子文件夹”（文件分散在机构子目录时）；③ 核对待处理文件；④ 点击“汇总校验结果说明”。
输出：执行结果目录下的“校验结果与报送说明汇总”工作簿，以及可选运行日志。
提示：模板表结构不匹配的文件会跳过，并在运行记录中提示。
```

#### 3. 组合联合核查表

适用：文件名规范、需要把同一机构的多类报表（如个人贷款和单位贷款）合成一本，供制作联合模板或跨表核查。

```text
准备：源文件主名第一个下划线前必须是机构全称。
步骤：① 选择源数据目录；② 需要时勾选递归；③ 点击“组合联合核查表”。
输出：每家机构一本合并工作簿。
提示：同名工作表只保留先复制的一张；本流程不需要模板、外部文件或数据期。
```

#### 4. 自定义流程 / 自定义按钮

适用：需要把多个内置模块按自己的顺序组合，或使用“组合工作表”等没有主界面固定按钮的功能。

```text
步骤：① 设置中心 → 流程编排 → 新建流程；② 从可用模块添加所需模块，设定顺序、是否输出结果；③ 保存；④ 设置中心 → 自定义按钮，建立“按钮名称 → 流程名称”，并设为显示；⑤ 回到主界面点击新按钮运行。
提示：自定义流程至少要有一个启用模块选择“是否输出结果=是”。
```

#### 5. 组合工作表（通过自定义流程使用）

适用：不想按“文件名第一段=机构名”组合，而需要按名称/关键字或正则规则组合文件。

```text
步骤：① 设置中心 → 组合工作表分组方案，选择或新建方案并保存；② 设置中心 → 流程编排，新建流程并添加“组合工作表”模块；③ 设“是否输出结果=是”；④ 可在自定义按钮页建立入口；⑤ 选择源数据目录后运行该按钮。
提示：本流程只需要源数据目录和递归选项，不需要模板、外部文件或数据期。
```

### 进阶：模块说明（默认折叠）

模块说明按功能类别分组；每项只用“作用 / 需要准备 / 常用放置位置”三行，不写程序术语。内容如下：

| 模块 | 作用 | 需要准备 | 常用放置位置 |
| --- | --- | --- | --- |
| 检查校验区域 / 检查表结构区域 / 检查汇总区域 | 先确认模板命名区域存在 | 模板已定义对应命名区域 | 流程开头 |
| 表结构比对 | 比对模板与报送文件的固定表头 | 表结构区域 | 审核、汇总前 |
| 外部文件添加 | 把外部辅助表复制进审核副本 | 外部 Excel 文件 | 公式校验复制之前 |
| 公式校验复制 | 将模板校验公式复制到审核副本并计算 | 校验区域 | 外部文件添加之后 |
| 校验结果提取 | 将触发的公式结果汇总为本期审核结果 | 校验区域 | 审核流程最后，通常输出结果=是 |
| 任意行汇总 | 汇总不固定行数的明细 | 任意行汇总区域、表头区域 | 说明汇总流程 |
| 固定行汇总 | 汇总固定位置的行 | 固定行汇总区域、表头区域 | 固定报表汇总流程 |
| 汇总表合并 | 把表头一致的汇总表进一步合并 | 无额外命名区域 | 汇总完成后 |
| 组合联合核查表 | 按文件名第一段组合机构文件 | 规范文件名 | 独立流程 |
| 组合工作表 | 按已保存的名称/正则方案组合文件 | 分组方案 | 独立自定义流程 |

模块说明最后附一条简短规则：**模块由内置逻辑提供；日常使用优先运行“常用流程”。只有需要改变执行顺序或组合方式时，才创建自定义流程。**

- 打开时调用 `get_config_editor_data()`；加载中显示简洁 loading。
- 读取后复制为前端草稿，只在保存时写入。
- 关闭或取消时，有未保存改动则确认。
- 任务运行中禁用设置入口和保存。
- 弹窗、表格、说明均必须可滚动。
- 主界面不再保留“打开配置”按钮，也不要保留其右键菜单。

主界面原有“打开配置”入口及其右键菜单请完全移除；设置中心也不展示、预览或提供“历史审核说明”入口。历史说明文件已改为 `data/历史审核说明.xlsx`，用户直接使用 Excel 打开和维护。

不要实现任何右键菜单或右键继承逻辑。恢复默认设置是“常规设置”页底部的普通按钮。

## 常规设置页

三个选项即时保存，不纳入“保存自定义设置”：

1. 计算引擎：自动（推荐）/ Microsoft Excel / WPS 表格。
   - 自动优先 Excel，启动失败时尝试 WPS。
   - 切换：`update({calculationEngine: value})`。
2. 递归扫描子文件夹。
   - 汇总核查通常不需开启；机构说明文件分散时通常需要。
   - 切换：`update({recursive: checked})`。
3. 输出流程运行日志。
   - 关闭后不生成运行日志 xlsx，页面日志仍保留。
   - 切换：`update({writeFlowLogs: checked})`。
4. 显示自定义功能。
   - 控制主界面是否显示整块自定义功能按钮区；不改变各按钮自身“是否显示”配置。
   - 初始值读取 `state.showCustomFeatures`；切换调用 `update({showCustomFeatures: checked})`，再调用 `refreshCustomButtons()`。
5. 日志展示方式。
   - 使用下拉或单选：`简洁` / `全量`。
   - 仅控制页面日志是否显示后端标注为 detail 的明细；不影响任务运行、不影响“输出流程运行日志”开关、不影响 xlsx。
   - 保留现有前端过滤逻辑；无需新增后端接口。可用 localStorage 记住选择。

常规设置页最下方放置低频危险操作区：

```text
恢复默认设置
清空所有自定义模块、自定义流程和自定义按钮；
不会修改内置模块、内置流程、计算引擎、常用路径或历史审核说明。
```

点击必须中文二次确认，再调用：

```js
await pywebview.api.reset_config()
```

完成后重新调用 `get_config_editor_data()` 和 `refreshCustomButtons()`。

## 组合工作表分组方案页

新增设置页签“组合工作表分组方案”。它只作用于新的默认流程 **组合工作表**；原“组合联合核查表”继续固定按文件名下划线第一段分组，两个按钮并行保留，便于对比测试。

- 方案名称：普通文本，用于日志提示。
- 分组方式：下拉读取 `combineSheetsModes`，只保留“正则表达式”和“按名称分组”。
- `regex`：显示正则输入框。必须包含命名分组 `(?P<组合>...)`（也兼容 `group`）或至少一个括号分组；匹配出的内容就是输出工作簿的组合名称。
- `name`：可新增多条“组合名称 + 匹配名称/关键字”规则；匹配名称可用顿号/逗号分隔。一般用户只需填写机构全称即可，例如组合名称和匹配名称都填写“广东某某银行”。文件名中包含该名称的个人贷款、单位贷款等文件会合并为同一本。一个文件只能命中一条规则，未命中或同时命中由后端报错，前端不自行猜测。

界面顶部应有“已保存方案”列表、`新建方案`、`复制方案`、`删除方案`。**不要只用下拉框表示当前方案。** 每个方案名称左侧必须有清晰的单选圆点或勾选框：当前启用方案显示为已选状态并附“当前使用”标签；点击其他方案的单选圆点/整行后，立即切换编辑内容，并在顶部明确提示“已选择，保存后生效”。保存时将其 `id` 写入 `activeCombineSheetsPlanId`。每个方案有唯一的内部 `id` 和用户可编辑的方案名称；至少保留一个方案，不能删除当前唯一方案。

设置草稿必须随普通保存一同传入 `combineSheetsPlans` 和 `activeCombineSheetsPlanId`。显示一句简短提示：**组合工作表只需要源数据目录和递归选项；它不读取模板、外部文件或数据期。**

正则输入框下方固定显示这段帮助文字，方便用户随时查阅：

```text
分组规则说明：正则匹配“文件主名”（不含 .xlsx）。必须用 (?P<组合>...) 标出组合名称；程序将同一组合名称的文件合并为一本。
示例 1：(?P<组合>.+?)_金融基础数据-        提取“金融基础数据-”前的文字。
示例 2：金融基础数据-(?P<组合>个人贷款|单位贷款)  按报表类型分组。
```

按名称分组的规则表显示为两列“组合名称 / 匹配名称（可多个）”，可逐行新增、删除；**组合名称相同的文件会合并为同一组**。例如组合名称填写“某银行联合核查”，匹配名称填写“某银行”，即可把文件名包含“某银行”的多张报表组合在一起。

主界面保留现有“组合联合核查表”按钮，并新增“组合工作表”按钮。二者均使用：

```js
await pywebview.api.start_flow_with_state(flowName, values(), checkedPaths)
```

不要对这两个流程调用自动识别模板，也不要要求用户填写模板路径。

## 模块页：内置模块只读 + 自定义模块可编辑

展示 `data.modules`，可以用卡片或精简表格。显示功能名、功能类型、命名区域名、是否限定工作簿、工作簿关键字、备注。

内置模块（`只读 === '是'`）不提供任何编辑或删除入口。自定义模块（`只读 === '否'`）允许编辑、删除；提供 `+ 新增自定义模块`。

新增自定义模块字段：

| 字段 | 规则 |
| --- | --- |
| 功能名 | 用户自定义，必须唯一，不能与内置模块重名 |
| 功能类型 | 下拉选择，选项仅来自 `featureTypes`，不可手输新类型 |
| 命名区域名 | 用户自定义，可填多个，以“、”或“，”分隔 |
| 是否限定工作簿 | 是/否；为“是”时必须填写工作簿关键字 |
| 工作簿关键字 | 用户自定义 |
| 备注 | 用户自定义 |

顶部说明：

```text
内置模块由程序维护，不能修改。需要不同命名区域、不同限定工作簿或不同说明时，新建自定义模块并选择对应功能类型。
```

所有内置模块和自定义模块均是流程编排页“可用模块”的数据来源。

## 流程编排页

### 内置流程

- 在流程选择器中列出 `defaultFlowNames`。
- 点击后展示 `defaultFlows` 中同名步骤。
- 显示“内置流程，只读”标签。
- 禁止编辑、删除、排序、保存到内置流程。
- 提供 `复制为自定义流程`：用户填写新流程名后，将默认步骤复制到 `customFlows`。

### 自定义流程

提供：新建流程、删除、复制、添加模块、删除步骤、上移/下移步骤。

建议左侧为可用模块，中间为当前流程步骤，右侧为选中步骤设置。HTML5 拖拽不是必须；若成本高，用添加/上移/下移即可，不能引入外部库。

步骤字段：

| 字段 | 交互 |
| --- | --- |
| 流程名 | 新建或复制时填写；同流程所有步骤相同 |
| 顺序 | 自动 10、20、30…；调整位置后重算 |
| 功能名 | 从内置模块选择 |
| 启用 | 是/否 |
| 失败后处理 | 停止/跳过 |
| 是否输出结果 | 是/否 |
| 输出文件名 | 可空 |
| 处理对象 | 可空；仅可选当前流程中排在此前的模块 |
| 备注 | 可空 |

新增步骤默认：启用=是、失败后处理=停止、是否输出结果=否。

帮助文字：

```text
处理对象留空时使用源数据。
选择前序“外部文件添加”或“公式校验复制”时，后续步骤读取其产出的副本。
每个自定义流程至少要有一个启用步骤输出结果。
“组合联合核查表”必须单独成一个流程，不能和其他模块混用。
```

## 自定义按钮页

字段为：按钮名称、流程名称、是否显示、备注。

- 流程名称下拉来源：内置流程 + 自定义流程。
- 支持新增、删除、编辑。
- “是否显示=是”时，按钮平铺显示在主界面自定义操作区。
- 说明：按钮只是入口，实际执行的是关联流程。

每个按钮的“是否显示”只控制该按钮自身；全局“显示自定义功能”开关位于常规设置页。主界面不要再提供独立设置入口。


## 保存与错误处理

保存只传：

```js
{
  customModules: draft.customModules,
  customFlows: draft.customFlows,
  buttons: draft.buttons
}
```

先调用 `validate_config_editor_draft`。校验失败时在设置面板内显示完整中文错误列表；校验通过才调用 `save_config_editor_draft`。

成功后提示“自定义流程和按钮已保存”，用 `result.data` 覆盖本地草稿，并调用 `refreshCustomButtons()`。

## 禁止事项

- 不显示、读取或编辑“历史审核结果”。
- 不编辑内置模块和内置流程。
- 不在浏览器侧读写 xlsx/json。
- 不修改审核、模板识别、运行日志、Win7 单次调用入口等现有逻辑。

## 进阶使用密码：本次运行只验证一次

“进阶使用”密码门改为**当前工具窗口会话一次验证**：用户第一次输入正确密码后，关闭设置中心、再次打开设置中心、切换普通页与进阶页，均不得再次要求密码；仅关闭工具窗口或刷新本地页面后才重新锁定。

实现要求：

```js
// 放在 CFG 外的模块级变量；不要写入 localStorage、用户设置或任何文件。
let advancedSessionUnlocked = false
```

- `openSettings()` 不得再把 `CFG.advancedUnlocked` 强制设为 `false`，应恢复为 `advancedSessionUnlocked`。
- `closeSettings()` 不得清除本次会话解锁状态。
- `confirmAdvanced()` 密码正确后，同时设置 `advancedSessionUnlocked = true` 和 `CFG.advancedUnlocked = true`。
- 关闭应用/刷新页面时 JavaScript 内存自然清空，下一次运行重新输入密码。
- 这只是防止普通用户误改进阶配置的操作门槛，不要在界面中把它描述为安全加密或权限系统。
